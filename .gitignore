# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs & notebook exports (regenerable)
*.log
*.html

# Web demo (app.py) runtime cache — matplotlib/fontconfig
.cache/

# Stage 1 · data — regenerate with data/prepare_data.py
data/recipes_clean.csv

# Stage 2 · yolo — weights you provide + the cloned upstream repo (large/external)
yolo/weights/*.pt
yolo/yolov5/
# (yolo/yolo_vocab_mapping.json IS committed: small, generated by build_mapping.py)

# Stage 3 · model — trained recommender (regenerate with model/src/run_demo.py)
model/models/*.pkl

# (model/figures/*.png ARE committed: small, needed for the README to render)
