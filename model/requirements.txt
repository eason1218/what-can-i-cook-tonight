# Core model + recommender
scikit-learn>=1.4      # LatentDirichletAllocation (phi point estimate + Bootstrap refits)
joblib>=1.3            # Bootstrap parallelism
numpy>=2.1
pandas>=2.2
scipy>=1.16            # Hungarian topic alignment + KL / softmax
inflect>=7.5

# Visualization
matplotlib>=3.10
seaborn>=0.13

# Data preparation (downloads Food.com)
kagglehub>=1.0

# Notebooks (optional: to run / re-execute the .ipynb files)
jupyter
nbconvert
ipykernel
