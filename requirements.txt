# End-to-end pipeline deps (data → yolo → model). See per-stage notes below.

# --- Stage 3 · recommender (model/) -----------------------------------------
scikit-learn>=1.4      # LatentDirichletAllocation (phi point estimate + Bootstrap refits)
joblib>=1.3            # Bootstrap parallelism
numpy>=2.1
pandas>=2.2
scipy>=1.16            # Hungarian topic alignment + KL / softmax
inflect>=7.5           # ingredient singularization
matplotlib>=3.10       # figures
seaborn>=0.13

# --- Stage 1 · data (data/) -------------------------------------------------
kagglehub>=1.0         # download Food.com

# --- Stage 2 · yolo (yolo/) -------------------------------------------------
torch>=2.0             # YOLOv5 inference
torchvision>=0.15
opencv-python>=4.8     # image IO for YOLO
pillow>=10.0
rapidfuzz>=3.0         # YOLO label -> ingredient fuzzy mapping
# the detector backend is the ultralytics/yolov5 repo, cloned into yolo/yolov5:
#   git clone https://github.com/ultralytics/yolov5 yolo/yolov5
# (it has its own requirements.txt; the packages above cover CPU inference)

# --- Web demo (app.py) ------------------------------------------------------
gradio>=5            # interactive UI over the same recommender as run_pipeline.py

# --- Notebooks (optional) ---------------------------------------------------
jupyter
nbconvert
ipykernel
